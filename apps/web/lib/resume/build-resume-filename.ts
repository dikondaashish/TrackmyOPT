// Keep editor downloads, persisted resumes, and extension uploads identical.
export {
    buildResumePdfFilename,
    extractNameFromLatex,
    extractRoleFromLatex,
} from '../../../extension/src/resume-filename';
