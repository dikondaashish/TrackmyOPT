/**
 * DOM identifiers the job-portal widget owns. Kept in one place so modules can
 * reference them without importing the widget itself.
 */


export const WIDGET_ROOT_ID = 'tmo-job-tracker-widget';

export const WIDGET_THEME_STYLE_ID = 'tmo-widget-theme-tokens';

export const WIDGET_THEME_SCOPE_CLASS = 'tmo-widget-theme-scope';

export const ARTIFACT_STALE_BANNER_CLASS = 'tmo-artifact-stale-banner';

export const ARTIFACT_INACTIVE_FALLBACK_CLASS = 'tmo-artifact-inactive-fallback';

export const RESUME_PANEL_CLASS = 'tmo-resume-panel';
